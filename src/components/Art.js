import React from 'react';

const Art = () => (
  <div style={{
    width: '470px',
    margin: 'auto',
    textAlign: 'center',
    padding: '10px',
  }}>
    <h1>Art</h1>
    <p>Add photos of art, with brief synopsis and background for each</p>
  </div>
)

export default Art;