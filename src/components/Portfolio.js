import React from 'react';

const Portfolio = () => (
  <div style={{
    width: '470px',
    margin: 'auto',
    textAlign: 'center',
    padding: '10px',
  }}>
  <h1>Portfolio</h1>
  <p>Web development projects (not a ton right now)</p>
  </div>
)

export default Portfolio;