import React from 'react';

const Resume = () => (
  <div style={{
    width: '470px',
    margin: 'auto',
    textAlign: 'center',
    padding: '10px',
  }}>
  <h1>Resume</h1>
  <p>Put resume here, instead of a link to resume</p>
  </div>
)

export default Resume;